# Secrets — never commit
.env
*.env
!.env.example
# MCP servers, same reason: the `manual.headers` block holds API keys in plain text.
# Committed as `mcp.example.json`; whichever starts first copies it to `mcp.json`.
mcp.json

# Local media / conversation store
memory/
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Frontend
node_modules/

.vite/

# Editors / OS
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
.idea/
Thumbs.db
.DS_Store
