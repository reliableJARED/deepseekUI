# ── Core (required) ───────────────────────────────────────────────────────────
httpx>=0.27            # the only dependency of the deepseek_client package itself

# ── Local server ──────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12   # multipart/form-data, for media uploads

# ── Media tools (optional: install only what you enable) ──────────────────────
pillow>=10.4               # still-image resize / compress / format conversion
opencv-python>=4.10        # video frame extraction and frame-count reduction

# ── MCP client (optional) ─────────────────────────────────────────────────────
mcp>=2.2                   # official Python SDK; streamable HTTP transport

# ── Web search (MCP server, mcp_server/mcpserver.py) ──────────────────────────
# Only needed by the tool server, not by the app. web_search runs TWO indexes in
# parallel and returns both:
#   * google-genai — grounded in Google Search. Writes the summary and supplies
#     the source URLs. Free tier (500 grounded requests/day) needs only an API
#     key from https://aistudio.google.com/apikey — no billing account.
#   * ddgs — DuckDuckGo's raw result list. No API key, and no model in the path,
#     so unlike Gemini's summary it cannot decline a query on subject-matter
#     grounds. Optional at runtime: without it, search is Gemini-only again.
google-genai>=1.0
ddgs>=9.0

# ── Not required ──────────────────────────────────────────────────────────────
# Shipped as part of the standard library / already vendored:
#   python-dotenv  -> replaced by deepseek_client.config.load_env_file
#   openai         -> replaced by deepseek_client itself
